{
  "name": "scraper",
  "version": "1.0.0",
  "description": "A Python web scraper that uses Playwright to connect to existing Edge browser sessions, allowing you to manually authenticate and then scrape protected content.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  },
  "dependencies": {
    "duckdb": "^1.4.0"
  }
}
